module.exports = {
  // The production branch that Pages will deploy from
  "build": {
    "production": {
      // The command to build your site
      "command": "npm run build-js",
      // The directory where your built site is located
      "outputDir": "dist"
    }
  }
}
